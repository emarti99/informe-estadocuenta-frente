import { FormatoDatosNulosPipe } from './formato-datos-nulos.pipe';

describe('FormatoDatosNulosPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoDatosNulosPipe();
    expect(pipe).toBeTruthy();
  });
});
